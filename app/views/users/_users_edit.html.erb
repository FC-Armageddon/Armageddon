
<%= form_for(user) do |f| %>
    <label>Name</label>
      <!-- f.text_field :name, autofocus: true, placeholder:"名前" -->
      </br>
      <%= attachment_image_tag user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg" %>
      </br>

    <label>ProfileImage</label>
      <%= f.attachment_field :profile_image, placeholder: "プロフィール画像" %>
      </br>

    <label>姓</label>
      <%= f.text_field :first_name, autofocus: true %>
      </br>

    <label>名</label>
      <%= f.text_field :last_name, autofocus: true %>
      </br>

    <label>カナ姓</label>
      <%= f.text_field :kana_first_name, autofocus: true %>
      </br>

    <label>カナ名</label>
      <%= f.text_field :kana_last_name, autofocus: true %>
      </br>

    <label>郵便番号</label>
      <%= f.text_field :postal_code, autofocus: true %>
      </br>

    <label>住所</label>
      <%= f.text_field :address, autofocus: true %>
      </br>

    <label>メールアドレス</label>
      <%= f.text_field :email, autofocus: true %>
      </br>

  <label>配送先</label>
  <div class="tags">
    <%= f.fields_for :destinations do |f| %>
    <%= f.label "Destination" %>
  　　<%= render "destination_fields", f: f %>
    <% end %>
    <div class="links">
      <%= link_to_add_association "配送先を追加", f, :destinations %>
    </div>
  </div>

  <% if admin_signed_in? %>
     <%= f.submit "更新" %>
     <%= link_to "退会", admins_deleted_flag_path, method: :patch %>

  <% else %>
     <%= f.submit "更新" %>
     <%= link_to "退会", deleted_flag_path, method: :patch %>
  <% end %>

<% end %>